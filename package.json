{
  "name": "catbot",
  "version": "1.0.0",
  "description": "Sends a cat image in the Discord channel when the |cat command is used.",
  "main": "index.js",
  "scripts": {
    "start": "node -r dotenv/config index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.0",
    "discord.js": "^12.5.1",
    "dotenv": "^8.2.0"
  }
}
